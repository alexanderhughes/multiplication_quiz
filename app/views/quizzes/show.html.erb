<% provide(:title, "Quiz") %>
<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %></title>
  </head>
  <body>
	  <h1>Here's the quiz!</h1>
	  <p>What is <%= @quiz.questions[0][:num1] %> x <%= @quiz.questions[0][:num2] %>?</p>
	  <%= form_tag( { controller: "questions", action: "update", id: @quiz.questions[0].id }, method: "put") do %>
	    <%= label_tag(:answer, "Answer:") %>
	    <%= text_field_tag(:answer) %>
	    <%= submit_tag("Submit") %>
	  <% end %>
  </body>
</html>